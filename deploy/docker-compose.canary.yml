services:
  cli-proxy-api-canary:
    image: ${CLIPROXYAPI_IMAGE:-cliproxyapi-local:v7.2.94-bravo-native0.5.0}
    pull_policy: never
    container_name: CLIProxyAPI-Canary
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "/CLIProxyAPI/cliproxy-healthcheck"]
      interval: 15s
      timeout: 4s
      retries: 5
      start_period: 10s
    ports:
      - "${CLIPROXYAPI_BIND_ADDRESS:-127.0.0.1}:${CLIPROXYAPI_CANARY_PORT:-18319}:8317"
    volumes:
      - "./config.yaml:/CLIProxyAPI/config.yaml"
      - "./auths:/root/.cli-proxy-api"
      - "./bravo-data:/CLIProxyAPI/bravo-data"
      - "./logs:/CLIProxyAPI/logs"
      - "./plugins:/CLIProxyAPI/plugins"
