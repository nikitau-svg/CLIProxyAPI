name: Retired one-shot Bravo Spark generator

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired in favor of the consolidated Bravo 0.7 patch.'
